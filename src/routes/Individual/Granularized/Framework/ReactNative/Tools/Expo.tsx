import { RouteObject } from "react-router-dom";

import CLI from '@/routes/Individual/Granularized/Framework/ReactNative/Tools/Expo/CLI';

const Expo: RouteObject[] = [
    ...CLI
];

export default Expo;